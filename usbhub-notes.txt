What YOU need to do (important)
1. Get your real paths

Run:

ls /dev/v4l/by-path/

2. Assign them like this:

Example:

1.2 → FRONT
1.3 → LEFT
1.4 → RIGHT

Then update:

cam_front = cv2.VideoCapture("your-front-path")
How to access now
http://raspberrypi.local:5000/front
http://raspberrypi.local:5000/left
http://raspberrypi.local:5000/right

(or use IP if needed)